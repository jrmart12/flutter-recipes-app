import 'package:injectable/injectable.dart';

@module
abstract class RegisterModule {
  // Example: Register authentication dependencies
  // Example: Register recipes dependencies
  // Example: Register user recipes dependencies
}